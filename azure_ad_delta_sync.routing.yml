azure_ad_delta_sync.settings:
  path: '/admin/config/azure_ad_delta_sync'
  defaults:
    _form: '\Drupal\azure_ad_delta_sync\Form\SettingsForm'
    _title: 'Settings for azure_ad_delta_sync'
  requirements:
    _permission: 'administer site configuration'
